name: ai_research_assistant
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - transformers
      - llama-index
      - sentence-transformers
      - faiss-cpu
      - chromadb
      - streamlit
      - pypdf
      - python-dotenv
      - pytest
